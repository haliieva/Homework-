'use strict';

let x = +prompt('Enter x'),
    y = +prompt('Enter y');

let product = x * y,
    sum = x + y,
    quotient = x / y,
    diff = x - y;

alert('Product of two numbers: ' + product)
alert('The sum of two numbers: ' + sum)
alert('Quotient of two numbers: ' + quotient)
alert('Difference of two numbers: ' + diff)
